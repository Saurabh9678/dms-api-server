$kind: http-example
name: Success
order: 1000
request:
  url: '{{base_url}}/api/v1/auth/verify-otp'
  method: POST
response:
  statusCode: 200
  statusText: OK
  body:
    type: json
    content: |-
      {
        "success": true,
        "message": "OTP verified successfully",
        "data": {
          "accessToken": "...",
          "refreshToken": "...",
          "expiresIn": 3600,
          "tokenType": "Bearer",
          "required_name": true
        }
      }
