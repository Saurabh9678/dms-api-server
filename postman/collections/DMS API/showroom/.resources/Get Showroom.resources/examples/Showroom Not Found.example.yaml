$kind: http-example
request:
  url: "{{base_url}}/api/v1/showroom/999"
  method: GET
  headers:
    Authorization: Bearer {{access_token}}
    X-Platform: web
    X-Device-Id: browser-1
response:
  statusCode: 404
  statusText: Not Found
  body:
    type: text
    content: |-
      {
        "success": false,
        "error": {
          "code": "SHOWROOM_NOT_FOUND",
          "message": "invalid request"
        }
      }
order: 2
