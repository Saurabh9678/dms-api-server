$kind: http-example
request:
  url: "{{base_url}}/api/v1/vehicle/listing?status=ready_for_sale&page=1&limit=20"
  method: GET
  headers:
    X-Platform: web
    X-Device-Id: device-001
    Authorization: Bearer {{access_token}}
response:
  statusCode: 400
  statusText: Bad Request
  body:
    type: text
    content: |-
      {
        "success": false,
        "error": {
          "code": "INVALID_REQUEST",
          "message": "invalid request"
        }
      }
order: 2000
